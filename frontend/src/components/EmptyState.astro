---
/**
 * Empty State Component
 *
 * Displays a friendly empty state with icon, title, and description
 * Used when lists or content areas are empty
 */

interface Props {
  icon?: string;
  title: string;
  description?: string;
  actionText?: string;
  actionHref?: string;
}

const { icon = '💬', title, description, actionText, actionHref } = Astro.props;
---

<div class="flex flex-col items-center justify-center p-8 text-center">
  <div class="text-6xl mb-4">{icon}</div>
  <h3 class="text-lg font-semibold text-gray-900 mb-2">{title}</h3>
  {description && <p class="text-sm text-gray-600 mb-4 max-w-sm">{description}</p>}
  {actionText && actionHref && (
    <a
      href={actionHref}
      class="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition text-sm font-medium"
    >
      {actionText}
    </a>
  )}
</div>
